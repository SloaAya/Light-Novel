# 运行时依赖：无。
#   * 二维码（可选）：segno 或 qrcode，未安装会自动跳过打印二维码。
#   * 图形控制面板用标准库 tkinter（部分精简版 Python 不含，需安装 python3-tk）。
#
# 打包 exe 时需要（仅构建机）：
pyinstaller>=6
#
# UI 探针（可选，仅开发时）：tests/ui_probe.py 用真实浏览器核对页面显隐行为。
#   python tests/ui_probe.py；无需 playwright install —— 脚本走 channel="msedge"
#   复用系统 Edge。没装 playwright 时脚本会自己跳过。
playwright
